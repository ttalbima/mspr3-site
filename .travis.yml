language: php
php:
  - '7.4'
    

script:
   - composer install
   - vendor/bin/simple-phpunit

deploy:
  provider: heroku
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  local_dir: build
  on:
    branch: master